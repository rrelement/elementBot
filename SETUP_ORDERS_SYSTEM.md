# Инструкция по настройке системы заказов

## Обзор

Система разделена на три бота:
1. **Основной бот** (`elementBot.py`) - основной бот для клиентов
2. **Бот заказов** (`orders_bot.py`) - для заказов на сведение и биты на заказ
3. **Бот покупок** (`beats_purchases_bot.py`) - для покупки готовых битов

## Настройка

### 1. Создание ботов

Создайте два новых бота через [@BotFather](https://t.me/BotFather):
- Бот для заказов (например, `@YourOrdersBot`)
- Бот для покупок (например, `@YourPurchasesBot`)

### 2. Настройка .env файла

Добавьте в файл `.env` следующие переменные:

```env
ORDERS_BOT_TOKEN=ваш_токен_бота_заказов
PURCHASES_BOT_TOKEN=ваш_токен_бота_покупок
ADMIN_ID=ваш_telegram_id
```

### 3. Установка зависимостей

Все зависимости уже должны быть установлены, но убедитесь, что установлены:
- `aiogram`
- `python-dotenv`

### 4. Запуск ботов

Запустите три бота в отдельных терминалах:

**Терминал 1 - Основной бот:**
```bash
python elementBot.py
```

**Терминал 2 - Бот заказов:**
```bash
python orders_bot.py
```

**Терминал 3 - Бот покупок:**
```bash
python beats_purchases_bot.py
```

## Функционал

### Бот заказов (`orders_bot.py`)

- Получает заказы на сведение и биты на заказ из основного бота
- Нумерация заказов: "Бит на заказ №1", "Заказ на Сведение №1" и т.д.
- Команды:
  - `/start` - информация о боте
  - `/orders` - показать все заказы
  - `/pending` - показать ожидающие заказы
  - `/in_progress` - показать заказы в работе
- Управление заказами через кнопки:
  - ✅ Принять / ❌ Отклонить
  - 🔨 Начать работу
  - ✅ Завершить
  - ✅ Отметить вторую оплату
  - 📋 История

### Бот покупок (`beats_purchases_bot.py`)

- Получает покупки готовых битов из основного бота
- Нумерация покупок: "Покупка бита №1", "Покупка бита №2" и т.д.
- Команды:
  - `/start` - информация о боте
  - `/purchases` - показать все покупки
  - `/pending` - показать ожидающие оплаты
  - `/paid` - показать оплаченные покупки
- Управление покупками через кнопки:
  - ✅ Отметить оплату
  - 📤 Отправить файл
  - 📋 История

### Система оплаты 50/50 для заказов

Для заказов (сведение и бит на заказ) реализована система оплаты 50/50:

1. **Первая оплата (50%)** - клиент платит сразу после согласования цены
2. **Вторая оплата (50%)** - клиент платит после получения готового файла

После отправки готового файла клиенту:
- Добавляется кнопка "💬 Связаться (для правок)"
- Клиент может запросить правки
- После проверки файла клиент должен оплатить оставшиеся 50%

### Кнопка "Связаться"

При отправке готового файла клиенту автоматически добавляется кнопка "💬 Связаться (для правок)".

При нажатии на кнопку:
- Клиент может написать, какие правки нужны
- Сообщение передается админу
- Админ может связаться с клиентом для обсуждения правок

## Структура данных

Заказы хранятся в файле `orders.json`:
```json
{
  "custom_orders": [...],
  "mixing_orders": [...],
  "next_custom_id": 1,
  "next_mixing_id": 1
}
```

Покупки хранятся в файле `beats_purchases.json`:
```json
{
  "purchases": [...],
  "next_id": 1
}
```

## Статусы заказов

- `pending` - Ожидает принятия
- `accepted` - Принят
- `in_progress` - В работе
- `first_payment_received` - Первая оплата получена (50%), файл отправлен, ожидается вторая оплата
- `completed` - Выполнен (вторая оплата получена)
- `rejected` - Отклонен

## Статусы покупок

- `pending_payment` - Ожидает оплаты
- `payment_received` - Оплата получена
- `file_sent` - Файл отправлен
- `completed` - Завершена

## Важные замечания

1. **Все три бота должны работать одновременно** для полной функциональности
2. **Боты заказов и покупок должны быть запущены от имени админа** (ADMIN_ID)
3. **Файлы `orders.json` и `beats_purchases.json` создаются автоматически** при первом запуске
4. **При изменении ADMIN_ID** обновите его во всех трех файлах или используйте переменную окружения

## Устранение неполадок

### Бот не получает заказы
- Проверьте, что токены ботов указаны правильно в `.env`
- Убедитесь, что все три бота запущены
- Проверьте логи на наличие ошибок

### Заказы не сохраняются
- Проверьте права доступа к файлам `orders.json` и `beats_purchases.json`
- Убедитесь, что файлы созданы и доступны для записи

### Ошибки при отправке в другие боты
- Проверьте, что токены ботов корректны
- Убедитесь, что боты запущены и работают
- Проверьте логи на наличие ошибок подключения

























