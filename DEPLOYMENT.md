# 🚀 Инструкция по развертыванию на сервере

## 📋 Требования

- Python 3.9 или выше
- Linux сервер (Ubuntu/Debian рекомендуется)
- Доступ к серверу с правами sudo
- Telegram боты созданы через @BotFather

## 🔧 Подготовка сервера

### 1. Установка Python и зависимостей

```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем Python и pip
sudo apt install python3 python3-pip python3-venv -y

# Устанавливаем системные зависимости для librosa
sudo apt install libsndfile1 ffmpeg -y
```

### 2. Клонирование/загрузка проекта

```bash
# Создаем директорию для проекта
mkdir -p ~/elementBot
cd ~/elementBot

# Загрузите все файлы проекта в эту директорию
# (через git, scp, или другой способ)
```

### 3. Создание виртуального окружения

```bash
cd ~/elementBot
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

```bash
# Копируем пример файла
cp .env.example .env

# Редактируем .env файл
nano .env
```

**Важно:** Заполните все переменные в `.env`:
- `TOKEN` - токен основного бота
- `ADMIN_ID` - ваш Telegram ID
- `ORDERS_BOT_TOKEN` - токен бота заказов
- `PURCHASES_BOT_TOKEN` - токен бота покупок
- `DEEPSEEK_API_KEY` - API ключ DeepSeek (опционально)
- `PROXY_URL` - прокси (опционально, если Telegram заблокирован)

### 5. Инициализация базы данных

```bash
# База данных создастся автоматически при первом запуске
# Но можно проверить миграцию, если есть старые данные:
python migrate_to_sqlite.py
```

## 🚀 Запуск ботов

### Вариант 1: Ручной запуск (для тестирования)

```bash
# Активируем виртуальное окружение
source venv/bin/activate

# Запускаем в отдельных терминалах или через screen/tmux
python elementBot.py
python orders_bot.py
python beats_purchases_bot.py
```

### Вариант 2: Скрипт запуска

```bash
# Делаем скрипты исполняемыми
chmod +x start_all_bots.sh stop_all_bots.sh

# Запускаем все боты
./start_all_bots.sh

# Останавливаем все боты
./stop_all_bots.sh
```

### Вариант 3: Systemd сервисы (рекомендуется для продакшена)

```bash
# Копируем service файлы
sudo cp elementbot.service /etc/systemd/system/
sudo cp ordersbot.service /etc/systemd/system/
sudo cp purchasesbot.service /etc/systemd/system/

# Редактируем пути в файлах
sudo nano /etc/systemd/system/elementbot.service
sudo nano /etc/systemd/system/ordersbot.service
sudo nano /etc/systemd/system/purchasesbot.service

# Замените в каждом файле:
# - your_user на ваше имя пользователя
# - /path/to/elementBot на реальный путь (например, /home/user/elementBot)

# Перезагружаем systemd
sudo systemctl daemon-reload

# Включаем автозапуск
sudo systemctl enable elementbot.service
sudo systemctl enable ordersbot.service
sudo systemctl enable purchasesbot.service

# Запускаем сервисы
sudo systemctl start elementbot.service
sudo systemctl start ordersbot.service
sudo systemctl start purchasesbot.service

# Проверяем статус
sudo systemctl status elementbot.service
sudo systemctl status ordersbot.service
sudo systemctl status purchasesbot.service
```

## 📊 Мониторинг и логи

### Systemd логи

```bash
# Просмотр логов основного бота
sudo journalctl -u elementbot.service -f

# Просмотр логов бота заказов
sudo journalctl -u ordersbot.service -f

# Просмотр логов бота покупок
sudo journalctl -u purchasesbot.service -f
```

### Логи скриптов

Если используете скрипты запуска, логи находятся в `logs/`:
```bash
tail -f logs/main_bot.log
tail -f logs/orders_bot.log
tail -f logs/purchases_bot.log
```

## 🔒 Безопасность

1. **Не коммитьте `.env` файл в git!**
   ```bash
   echo ".env" >> .gitignore
   ```

2. **Установите правильные права на файлы:**
   ```bash
   chmod 600 .env
   chmod 644 *.py
   ```

3. **Используйте firewall:**
   ```bash
   sudo ufw allow 22/tcp  # SSH
   sudo ufw enable
   ```

## 🔄 Обновление ботов

```bash
cd ~/elementBot
source venv/bin/activate

# Останавливаем боты
./stop_all_bots.sh  # или через systemd: sudo systemctl stop elementbot.service

# Обновляем код (через git или вручную)
# git pull  # если используете git

# Обновляем зависимости (если нужно)
pip install -r requirements.txt --upgrade

# Запускаем снова
./start_all_bots.sh  # или через systemd: sudo systemctl start elementbot.service
```

## ⚠️ Устранение проблем

### Боты не запускаются

1. Проверьте `.env` файл - все токены должны быть указаны
2. Проверьте логи на наличие ошибок
3. Убедитесь, что виртуальное окружение активировано
4. Проверьте права доступа к файлам

### Ошибки подключения к Telegram

1. Проверьте интернет-соединение
2. Если Telegram заблокирован, настройте `PROXY_URL` в `.env`
3. Проверьте правильность токенов ботов

### Проблемы с базой данных

1. Проверьте права на файл `bot_database.db`
2. Убедитесь, что директория доступна для записи
3. При необходимости пересоздайте БД (удалите `bot_database.db` и перезапустите)

## 📝 Проверка работоспособности

После запуска проверьте:

1. Отправьте `/start` в основной бот - должен ответить
2. Отправьте `/start` в бот заказов - должен показать меню
3. Отправьте `/start` в бот покупок - должен показать меню
4. Создайте тестовый заказ через основной бот
5. Проверьте, что заказ появился в боте заказов

## 🎯 Готово!

Если все проверки пройдены, боты готовы к работе. Удачной эксплуатации! 🚀


