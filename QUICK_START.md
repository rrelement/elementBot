# Быстрый старт

## ✅ Токены добавлены

Токены для ботов уже добавлены в файл `.env`:
- `ORDERS_BOT_TOKEN` - для бота заказов
- `PURCHASES_BOT_TOKEN` - для бота покупок

## 🚀 Запуск ботов

### Вариант 1: Автоматический запуск (Windows)

Просто запустите файл `start_all_bots.bat` - он откроет все три бота в отдельных окнах.

### Вариант 2: Ручной запуск

Откройте **три отдельных терминала** и запустите в каждом:

**Терминал 1 - Основной бот:**
```bash
python elementBot.py
```

**Терминал 2 - Бот заказов:**
```bash
python orders_bot.py
```

**Терминал 3 - Бот покупок:**
```bash
python beats_purchases_bot.py
```

## ✅ Проверка работы

1. **Основной бот** должен запуститься без ошибок
2. **Бот заказов** должен показать сообщение "Запуск бота для заказов..."
3. **Бот покупок** должен показать сообщение "Запуск бота для покупок..."

## 📋 Что дальше?

1. Отправьте команду `/start` в бот заказов - вы должны увидеть приветственное сообщение
2. Отправьте команду `/start` в бот покупок - вы должны увидеть приветственное сообщение
3. Протестируйте создание заказа через основной бот:
   - Выберите "Custom beat" или "Mixing"
   - Создайте заказ
   - Заказ должен прийти в бот заказов с номером (например, "Бит на заказ №1")

## ⚠️ Важно

- Все три бота должны работать **одновременно**
- Если один из ботов не запускается, проверьте токены в `.env`
- Если возникают ошибки, проверьте логи в терминалах

## 🔧 Команды ботов

### Бот заказов:
- `/start` - информация о боте
- `/orders` - все заказы
- `/pending` - ожидающие заказы
- `/in_progress` - заказы в работе

### Бот покупок:
- `/start` - информация о боте
- `/purchases` - все покупки
- `/pending` - ожидающие оплаты
- `/paid` - оплаченные покупки

## 📝 Примечания

- Заказы автоматически нумеруются: "Бит на заказ №1", "Заказ на Сведение №1" и т.д.
- Покупки автоматически нумеруются: "Покупка бита №1", "Покупка бита №2" и т.д.
- Все данные сохраняются в файлах `orders.json` и `beats_purchases.json`

























